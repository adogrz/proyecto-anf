import AppLayout from '@/layouts/app-layout';
import React from 'react';

export default function CreateRatio() {
    return (
        <AppLayout>
            <h1 className="text-xl font-semibold">Crear Ratio</h1>
            <p>Contenido del placeholder para esta página.</p>
        </AppLayout>
    );
}
